<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Movie vs Trivia!</title>
  <link class="style-sheet" rel="stylesheet" href="">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/background.css">
</head>

<body>
  
    <header>
      <nav class="navbar navbar-expand-lg bg-light">
        <div class="container-fluid">
          <h1 class="navbar-brand">Movie Trivia Vs</h1>
          <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link active" href="/">Home</a>
              </li>
              {{#if logged_in}}
              <li class="nav-item">
                <button class="nav-link btn" id="logout">Logout</button>
              </li>
              {{else}}
              <li class="nav-item">
                <a class="nav-link btn" href="/login">Login</a>
              </li>
              {{/if}}
            </ul>
          </div>
        </div>
    </header>
    <main class="container container-fluid" style="margin-top: 15rem;">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer>

    </footer>
  

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>